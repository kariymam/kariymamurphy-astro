---
layout: home.njk
---
{% set postsCount = collections.posts | length %}
{% set morePosts = postsCount - numberOfLatestPostsToShow %}
{% set latestPostsCount = postsCount | min(numberOfLatestPostsToShow) %}
{% set postsCount = collections.posts | length %}
  <div class="w-full">
    <div id="header" class="flex flex-row lg:top-0 lg:sticky z-20">
      {% include "header.njk" %}
    </div>
    {% if numberOfLatestPostsToShow%}
      <h1 class="mb-1 p-6 md:px-9 border-t-2 displayFont" aria-label="Latest {{ latestPostsCount }} Post{% if latestPostsCount != 1 %}s{% endif %}">
        Latest {{ latestPostsCount }} Post{% if latestPostsCount != 1 %}s{% endif %}
        </h1>
        {% else %}
        <h1 class="mb-1 p-6 md:px-9 border-t-2">
          {{ title }}
          </h1>
      {% endif %}
    {{ content | safe }}
    {% if morePosts > 0 %}
       <small>{{ morePosts }} more post{% if morePosts != 1 %}s{% endif %} can be found in <a href="/tags/">the archive</a>.</small>
    {% endif %}
  </div>