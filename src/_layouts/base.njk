<!DOCTYPE html>
<html lang="en">
    <head>
        {% include "head.njk" %}
        {% if site.env %}
            <!-- Capture the CSS content as a Nunjucks variable -->
            {% set css %}{% include "styles.min.css" %}{% endset %}
            <style>
                {{css | safe}}
            </style>
        {% else %}
            <link rel="stylesheet" href="/static/styles.min.css">
        {% endif %}

        <!-- Try and serve Google Fonts in the most efficent way possible -->
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link rel="preload" href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;500&family=DM+Sans:opsz,wght@9..40,400;9..40,700&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'"/>
        <noscript>
            <link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;500&family=DM+Sans:opsz,wght@9..40,400;9..40,700&display=swap" rel="stylesheet" type="text/css"/>
        </noscript>
    </head>
    <body>
        <a href="#main" class="skip-link">Skip to main content</a>
          {% include "header.njk" %}
          <main class="mx-auto {% if permalink !== "/" %}max-w-screen-xl{% endif %} w-full my-32">
            {% block intro %}{% endblock %}
            {% block latestPosts %}{% endblock %}
            {% if permalink !== '/' %}
            {{ content | safe }}
            {% endif %}
          </main>
          {% include "footer.njk" %}
        <div id="navScrim" aria-hidden="true" class="dialog-overlay" data-a11y-dialog-hide></div>
        <script src="/static/app.min.js" defer></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
        <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
        <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
    </body>
</html>