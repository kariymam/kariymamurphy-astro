<footer class="h-auto mt-8 sm:mt-0 lg:mx-auto lg:w-full max-w-screen-xl left-0 right-0 bottom-0 lg:fixed opacity-70">
  <div class="relative p-6">
      <nav id="footer-nav" aria-label="footer">
        <ul
          class="flex flex-col items-center text-sm gap-6 sm:flex-row sm:gap-8 sm:justify-start"
        >
          {% if layout == "post.njk" %}
          {%- if collections.posts %}
          {%- set previousPost = collections.posts | getPreviousCollectionItem %}
          {%- set nextPost = collections.posts | getNextCollectionItem %}
          {%- if nextPost or previousPost %}
          {%- if previousPost %}
          <li id="previousPost" class="flex w-full items-center min-w-max h-10">
            <div class="flex w-full items-center md:flex-row md:justify-start">
                <ion-icon name="arrow-back-outline"></ion-icon>
                <a href="{{ previousPost.url }}" class="displayFont">{{ previousPost.data.title }}</a>
              </div>
            </li>
          {% endif %}
          {%- if nextPost %}
          <li id="nextPost" class="flex items-center min-w-max h-10">
            <div class="flex items-center md:flex-row md:justify-end">
                <a href="{{ nextPost.url }}" class="displayFont">{{ nextPost.data.title }}</a>
                <ion-icon name="arrow-forward-outline"></ion-icon>
              </div>
            </li>
          {% endif %}
          {%- endif %}
          {%- endif %}
          {% endif %}
        </ul>
        <ul class="flex flex-col flex-wrap items-center text-sm gap-6 sm:flex-row sm:gap-8 sm:justify-start">
          <li class="flex items-center gap-1 decoration-transparent">
            <a class="transition hover:/75" href="/resume/">Contact me</a>
          </li>
        </ul>
      </nav>
  </div>
</footer>