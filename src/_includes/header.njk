  <header class="z-20 sticky bg-[var(--background)]{% if layout == "post.njk" and thumb %} lg:bg-transparent{% endif %} top-0 left-0 right-0 lg:absolute flex w-full max-w-screen-xl mx-auto border-b-2">
    <div class="w-full flex-col sm:flex-row sm:flex justify-center gap-2 sm:justify-start p-6">
      <a href="/" class="decoration-transparent {% if permalink !== "/" %}opacity-70{% endif %}">
      kariyma murphy
      </a>
      {% if title and permalink !== "/" %}
        <span class="hidden border-l-2 w-1 h-6 mx-1 md:block"></span>
        <h1 class="text-base">{{ title }}</h1>
      {% endif %}
    </div>
    <div
      class="w-auto sm:w-full ml-auto sm:items-center gap-8 p-6"
    >
      <div class="w-full p-0">
        <button
          id="burger"
          aria-controls="main-nav"
          aria-expanded="false"
          class="ml-auto relative block bg-[var(--primary)] p-2.5 border-2 rounded sm:hidden"
        >
          <span class="sr-only">Toggle menu</span>
          <svg
            id="burgerIcon"
            xmlns="http://www.w3.org/2000/svg"
            class="h-5 w-5"
            viewBox="0 0 24 24"
          >
            <rect class="line top"
                  width="20" height="2"
                  x="2" y="5" rx="1"></rect>
            <rect class="line middle"
                  width="20" height="2"
                  x="2" y="11" rx="1"></rect>
            <rect class="line bottom"
                  width="20" height="2"
                  x="2" y="17" rx="1"></rect>
          </svg>
        </button>
        {% include "_partials/nav.njk" %}
      </div>
    </div>
    <div id="headerScrim" class="absolute top-0 left-0 flex w-full h-full -z-10"></div>
  </header>
{% if layout == "post.njk" and thumb %}
  <div id="thumbHero">
    <div class="relative w-screen h-[500px] min-w-fit self-center">
      {% if thumb %}
      <div class="flex overflow-hidden absolute h-full w-full items-center justify-cente scrim3">
        {% image thumb, thumbAlt, "thumbImg w-screen" %}
      </div>
      {% endif %}
    </div>
  </div>
{% endif %}